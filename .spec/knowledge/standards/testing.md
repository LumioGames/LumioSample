---
name: testing
description: 测试与验收——测试分层政策、TDD 时机、验收 DoD 与验证证据;实现功能/修 bug 时查
metadata:
  type: doc
  status: 已交付
---

# 测试与验收（含 TDD 政策）

> 本文定**政策**（测什么、何时测、怎么算过）；“先写失败测试再实现”的**方法**在 `test-driven-development` 技能。

## 测试分层（通用政策）

- **单元测试**：默认层，随项目验证命令（`AGENTS.md`「收口门槛」）每次跑，快、无外部依赖。
- **集成测试**（真库 / 真服务）：显式触发，不进默认验证命令，保持收口快。
- **端到端 / E2E**：显式触发；关键主链路至少一条。

## 何时走 TDD（测试分级）

- 分级口径以插件注入的 `rules/dispatch.md`「编码约定 · 测试分级」为单一权威：纯文档 / 配置数据 / 注释 / 删除改动、或有效新增行 < 50 = **小任务**，其余 = **大任务**（`closeout-gate` 的有效行口径）。
- **大任务必须走**：新功能、改无测试保护的关键逻辑——需求先聊清，再先写失败测试。修 bug 同此分级：小修复以复现步骤验证症状消失代替新测试，大修复先写能复现的失败测试并留作回归。
- **小任务不走**：不要求先失败的测试、不为每处改动新增用例；收口只跑 lint 与既有测试一次。一次性脚本同此。豁免在交回物里声明。
- 写测试、加 mock、想给生产类加 test-only 方法前，先查 `test-driven-development` 技能的反模式清单（`testing-anti-patterns.md`）——测 mock 行为、test-only 方法入生产、不理解依赖就 mock、不完整 mock，一律禁止。

## 验证证据

形式要求以插件注入的 `rules/dispatch.md`「交回物格式」为单一权威——「已通过」三个字不是证据。

## 验收标准（Definition of Done）

- [ ] 收口门槛命令全绿（命令见 [`../../AGENTS.md`](../../AGENTS.md)「收口门槛」）。
- [ ] 大任务的新增 / 修改行为有测试覆盖，其 bug 修复留有回归测试。
- [ ] 无 lint / 类型错误、无调试残留。
- [ ] 相关知识文档已更新（见 [`workflow.md`](./workflow.md)）。

## 项目测试栈与命令（**落地必填**）

<!-- 落地项目在此填写：测试框架、各层运行命令、集成/E2E 的开关方式与环境要求。 -->
<!-- 例：单元 `pnpm test`；E2E `pnpm test:e2e`（需本地起服务）。 -->
