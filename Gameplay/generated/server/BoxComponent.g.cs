// <auto-generated/>
#nullable enable
using System;
using Lumio.GameRuntime.Ecs;

namespace Lumio.Sample.Gameplay.Components.Box;

public sealed partial class BoxComponent : IGeneratedComponent, IGeneratedSyncMetadata, IGeneratedOperationComponent
{
    partial void OnNameChanging(string old, string @new, ChangeReason reason);
    partial void OnNameChanged(string old, string @new, ChangeReason reason);
    partial void OnLockedChanging(bool old, bool @new, ChangeReason reason);
    partial void OnLockedChanged(bool old, bool @new, ChangeReason reason);
    partial void OnClientWrite(in SyncWrite w, ref bool accept);


    void IGeneratedComponent.BindFields(ISyncHost host)
    {
        Name = Name.Bound(host, this, 0, "BoxComponent.name");
        Locked = Locked.Bound(host, this, 1, "BoxComponent.locked");
        Inventory.Bound(host, this, "BoxComponent.inventory");
        Openers.Bound(host, this, "BoxComponent.openers");
    }

    void IGeneratedComponent.InvokePostAttribute() => PostAttribute();
    partial void PostAttribute();

    void IGeneratedComponent.InvokeFieldChanging(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnNameChanging((string)oldValue!, (string)newValue!, reason);
        if (ordinal == 1) OnLockedChanging((bool)oldValue!, (bool)newValue!, reason);
    }

    void IGeneratedComponent.InvokeFieldChanged(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnNameChanged((string)oldValue!, (string)newValue!, reason);
        if (ordinal == 1) OnLockedChanged((bool)oldValue!, (bool)newValue!, reason);
    }

    bool IGeneratedComponent.DispatchClientWrite(in SyncWrite write)
    {
        bool accept = true;
        OnClientWrite(in write, ref accept);
        return accept;
    }

    void IGeneratedComponent.DispatchServerRpc(string method, object?[] args)
        => ((IGeneratedOperationComponent)this).TryDispatchServerRpc(method, args, out _);

    bool IGeneratedOperationComponent.TryDispatchServerRpc(string method, object?[] args, out OperationExecutionOutcome outcome)
    {
        outcome = new(OperationOutcomeKind.OutcomeUnavailable, OperationCommitFact.Unknown, "operation_outcome_unavailable");
        outcome = new(OperationOutcomeKind.ProtocolReject, OperationCommitFact.NotApplied, "operation_unknown_method");
        return false;
    }

    void IGeneratedComponent.DispatchClientRpc(string method, object?[] args)
    {
    }

    void IGeneratedComponent.CapturePersist(IPersistWriter writer)
    {
        if (writer is IPredictionFieldWriter) return;
        writer.WriteString("BoxComponent.name", Name.Value);
        writer.WriteBoolean("BoxComponent.locked", Locked.Value);
        writer.WriteContainer("BoxComponent.inventory", Inventory);
    }

    void IGeneratedComponent.CaptureSync(IPersistWriter writer)
    {
        if (writer is IPredictionFieldWriter prediction)
        {
            prediction.WritePredictionField("BoxComponent.name", Name.Value);
            prediction.WritePredictionField("BoxComponent.locked", Locked.Value);
            prediction.WritePredictionField("BoxComponent.inventory", Inventory);
            prediction.WritePredictionField("BoxComponent.openers", Openers);
            return;
        }
        writer.WriteString("BoxComponent.name", Name.Value);
        writer.WriteBoolean("BoxComponent.locked", Locked.Value);
        if (writer is IContainerFieldWriter inventoryWriter) inventoryWriter.WriteContainer("BoxComponent.inventory", Inventory);
    }

    void IGeneratedComponent.RestorePersist(IPersistReader reader)
    {
        if (reader.TryReadString("BoxComponent.name", out string nameRestore))
            Name.SetSilent(nameRestore);
        if (reader.TryReadBoolean("BoxComponent.locked", out bool lockedRestore))
            Locked.SetSilent(lockedRestore);
        if (reader.TryReadContainer("BoxComponent.inventory", out object inventoryRestore))
            ((ISyncContainer)Inventory).AssignFromRemote(inventoryRestore);
    }

    object? IGeneratedComponent.ReadField(string fieldId)
    {
        if (string.Equals(fieldId, "name", StringComparison.Ordinal)) return Name.Value;
        if (string.Equals(fieldId, "locked", StringComparison.Ordinal)) return Locked.Value;
        if (string.Equals(fieldId, "inventory", StringComparison.Ordinal)) return Inventory;
        if (string.Equals(fieldId, "openers", StringComparison.Ordinal)) return Openers;
        return null;
    }

    void IGeneratedComponent.WriteField(string fieldId, object? value, bool silent)
    {
        if (string.Equals(fieldId, "name", StringComparison.Ordinal))
        {
            if (silent) Name.SetSilent((string)value!);
            else Name.Value = (string)value!;
            return;
        }
        if (string.Equals(fieldId, "locked", StringComparison.Ordinal))
        {
            if (silent) Locked.SetSilent((bool)value!);
            else Locked.Value = (bool)value!;
            return;
        }
        if (string.Equals(fieldId, "inventory", StringComparison.Ordinal))
        {
            ((ISyncContainer)Inventory).AssignFromRemote(value);
            return;
        }
        if (string.Equals(fieldId, "openers", StringComparison.Ordinal))
        {
            ((ISyncContainer)Openers).AssignFromRemote(value);
            return;
        }
    }

    bool IGeneratedSyncMetadata.TryGetSyncField(string fieldId, out ISyncField field)
    {
        if (string.Equals(fieldId, "name", StringComparison.Ordinal)) { field = Name; return true; }
        if (string.Equals(fieldId, "locked", StringComparison.Ordinal)) { field = Locked; return true; }
        field = null!;
        return false;
    }

    void IGeneratedComponent.ResetToDefault()
    {
        Name.SetSilent(string.Empty);
        Locked.SetSilent(false);
        ((ISyncContainer)Inventory).ResetForReuse();
        ((ISyncContainer)Openers).ResetForReuse();
    }
}
