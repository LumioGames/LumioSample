// <auto-generated/>
#nullable enable
using System;
using Lumio.GameRuntime.Ecs;

namespace Lumio.Sample.Gameplay.Components.Mining;

public sealed partial class PendingDigComponent : IGeneratedComponent, IGeneratedSyncMetadata, IGeneratedOperationComponent
{
    partial void OnActiveChanging(bool old, bool @new, ChangeReason reason);
    partial void OnActiveChanged(bool old, bool @new, ChangeReason reason);
    partial void OnTransactionChanging(string old, string @new, ChangeReason reason);
    partial void OnTransactionChanged(string old, string @new, ChangeReason reason);
    partial void OnVeinHexChanging(string old, string @new, ChangeReason reason);
    partial void OnVeinHexChanged(string old, string @new, ChangeReason reason);
    partial void OnSectionKeyChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnSectionKeyChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnCellOffsetChanging(int old, int @new, ChangeReason reason);
    partial void OnCellOffsetChanged(int old, int @new, ChangeReason reason);
    partial void OnCellXChanging(int old, int @new, ChangeReason reason);
    partial void OnCellXChanged(int old, int @new, ChangeReason reason);
    partial void OnCellYChanging(int old, int @new, ChangeReason reason);
    partial void OnCellYChanged(int old, int @new, ChangeReason reason);
    partial void OnCellZChanging(int old, int @new, ChangeReason reason);
    partial void OnCellZChanged(int old, int @new, ChangeReason reason);
    partial void OnAmountChanging(int old, int @new, ChangeReason reason);
    partial void OnAmountChanged(int old, int @new, ChangeReason reason);
    partial void OnStaminaCostChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnStaminaCostChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnSerialChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnSerialChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnClientWrite(in SyncWrite w, ref bool accept);


    void IGeneratedComponent.BindFields(ISyncHost host)
    {
        Active = Active.Bound(host, this, 0, "PendingDigComponent.active");
        Transaction = Transaction.Bound(host, this, 1, "PendingDigComponent.transaction");
        VeinHex = VeinHex.Bound(host, this, 2, "PendingDigComponent.veinHex");
        SectionKey = SectionKey.Bound(host, this, 3, "PendingDigComponent.sectionKey");
        CellOffset = CellOffset.Bound(host, this, 4, "PendingDigComponent.cellOffset");
        CellX = CellX.Bound(host, this, 5, "PendingDigComponent.cellX");
        CellY = CellY.Bound(host, this, 6, "PendingDigComponent.cellY");
        CellZ = CellZ.Bound(host, this, 7, "PendingDigComponent.cellZ");
        Amount = Amount.Bound(host, this, 8, "PendingDigComponent.amount");
        StaminaCost = StaminaCost.Bound(host, this, 9, "PendingDigComponent.staminaCost");
        Serial = Serial.Bound(host, this, 10, "PendingDigComponent.serial");
    }

    void IGeneratedComponent.InvokePostAttribute() => PostAttribute();
    partial void PostAttribute();

    void IGeneratedComponent.InvokeFieldChanging(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnActiveChanging((bool)oldValue!, (bool)newValue!, reason);
        if (ordinal == 1) OnTransactionChanging((string)oldValue!, (string)newValue!, reason);
        if (ordinal == 2) OnVeinHexChanging((string)oldValue!, (string)newValue!, reason);
        if (ordinal == 3) OnSectionKeyChanging((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 4) OnCellOffsetChanging((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 5) OnCellXChanging((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 6) OnCellYChanging((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 7) OnCellZChanging((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 8) OnAmountChanging((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 9) OnStaminaCostChanging((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 10) OnSerialChanging((ulong)oldValue!, (ulong)newValue!, reason);
    }

    void IGeneratedComponent.InvokeFieldChanged(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnActiveChanged((bool)oldValue!, (bool)newValue!, reason);
        if (ordinal == 1) OnTransactionChanged((string)oldValue!, (string)newValue!, reason);
        if (ordinal == 2) OnVeinHexChanged((string)oldValue!, (string)newValue!, reason);
        if (ordinal == 3) OnSectionKeyChanged((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 4) OnCellOffsetChanged((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 5) OnCellXChanged((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 6) OnCellYChanged((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 7) OnCellZChanged((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 8) OnAmountChanged((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 9) OnStaminaCostChanged((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 10) OnSerialChanged((ulong)oldValue!, (ulong)newValue!, reason);
    }

    bool IGeneratedComponent.DispatchClientWrite(in SyncWrite write)
    {
        bool accept = true;
        OnClientWrite(in write, ref accept);
        return accept;
    }

    void IGeneratedComponent.DispatchServerRpc(string method, object?[] args)
        => ((IGeneratedOperationComponent)this).TryDispatchServerRpc(method, args, out _);

    bool IGeneratedOperationComponent.TryDispatchServerRpc(string method, object?[] args, out OperationExecutionOutcome outcome)
    {
        outcome = new(OperationOutcomeKind.OutcomeUnavailable, OperationCommitFact.Unknown, "operation_outcome_unavailable");
        outcome = new(OperationOutcomeKind.ProtocolReject, OperationCommitFact.NotApplied, "operation_unknown_method");
        return false;
    }

    void IGeneratedComponent.DispatchClientRpc(string method, object?[] args)
    {
    }

    void IGeneratedComponent.CapturePersist(IPersistWriter writer)
    {
        if (writer is IPredictionFieldWriter) return;
        writer.WriteBoolean("PendingDigComponent.active", Active.Value);
        writer.WriteString("PendingDigComponent.transaction", Transaction.Value);
        writer.WriteString("PendingDigComponent.veinHex", VeinHex.Value);
        writer.WriteUInt64("PendingDigComponent.sectionKey", SectionKey.Value);
        writer.WriteInt32("PendingDigComponent.cellOffset", CellOffset.Value);
        writer.WriteInt32("PendingDigComponent.cellX", CellX.Value);
        writer.WriteInt32("PendingDigComponent.cellY", CellY.Value);
        writer.WriteInt32("PendingDigComponent.cellZ", CellZ.Value);
        writer.WriteInt32("PendingDigComponent.amount", Amount.Value);
        writer.WriteUInt64("PendingDigComponent.staminaCost", StaminaCost.Value);
        writer.WriteUInt64("PendingDigComponent.serial", Serial.Value);
    }

    void IGeneratedComponent.CaptureSync(IPersistWriter writer)
    {
        if (writer is IPredictionFieldWriter prediction)
        {
            prediction.WritePredictionField("PendingDigComponent.active", Active.Value);
            prediction.WritePredictionField("PendingDigComponent.transaction", Transaction.Value);
            prediction.WritePredictionField("PendingDigComponent.veinHex", VeinHex.Value);
            prediction.WritePredictionField("PendingDigComponent.sectionKey", SectionKey.Value);
            prediction.WritePredictionField("PendingDigComponent.cellOffset", CellOffset.Value);
            prediction.WritePredictionField("PendingDigComponent.cellX", CellX.Value);
            prediction.WritePredictionField("PendingDigComponent.cellY", CellY.Value);
            prediction.WritePredictionField("PendingDigComponent.cellZ", CellZ.Value);
            prediction.WritePredictionField("PendingDigComponent.amount", Amount.Value);
            prediction.WritePredictionField("PendingDigComponent.staminaCost", StaminaCost.Value);
            prediction.WritePredictionField("PendingDigComponent.serial", Serial.Value);
            return;
        }
    }

    void IGeneratedComponent.RestorePersist(IPersistReader reader)
    {
        if (reader.TryReadBoolean("PendingDigComponent.active", out bool activeRestore))
            Active.SetSilent(activeRestore);
        if (reader.TryReadString("PendingDigComponent.transaction", out string transactionRestore))
            Transaction.SetSilent(transactionRestore);
        if (reader.TryReadString("PendingDigComponent.veinHex", out string veinHexRestore))
            VeinHex.SetSilent(veinHexRestore);
        if (reader.TryReadUInt64("PendingDigComponent.sectionKey", out ulong sectionKeyRestore))
            SectionKey.SetSilent(sectionKeyRestore);
        if (reader.TryReadInt32("PendingDigComponent.cellOffset", out int cellOffsetRestore))
            CellOffset.SetSilent(cellOffsetRestore);
        if (reader.TryReadInt32("PendingDigComponent.cellX", out int cellXRestore))
            CellX.SetSilent(cellXRestore);
        if (reader.TryReadInt32("PendingDigComponent.cellY", out int cellYRestore))
            CellY.SetSilent(cellYRestore);
        if (reader.TryReadInt32("PendingDigComponent.cellZ", out int cellZRestore))
            CellZ.SetSilent(cellZRestore);
        if (reader.TryReadInt32("PendingDigComponent.amount", out int amountRestore))
            Amount.SetSilent(amountRestore);
        if (reader.TryReadUInt64("PendingDigComponent.staminaCost", out ulong staminaCostRestore))
            StaminaCost.SetSilent(staminaCostRestore);
        if (reader.TryReadUInt64("PendingDigComponent.serial", out ulong serialRestore))
            Serial.SetSilent(serialRestore);
    }

    object? IGeneratedComponent.ReadField(string fieldId)
    {
        if (string.Equals(fieldId, "active", StringComparison.Ordinal)) return Active.Value;
        if (string.Equals(fieldId, "transaction", StringComparison.Ordinal)) return Transaction.Value;
        if (string.Equals(fieldId, "veinHex", StringComparison.Ordinal)) return VeinHex.Value;
        if (string.Equals(fieldId, "sectionKey", StringComparison.Ordinal)) return SectionKey.Value;
        if (string.Equals(fieldId, "cellOffset", StringComparison.Ordinal)) return CellOffset.Value;
        if (string.Equals(fieldId, "cellX", StringComparison.Ordinal)) return CellX.Value;
        if (string.Equals(fieldId, "cellY", StringComparison.Ordinal)) return CellY.Value;
        if (string.Equals(fieldId, "cellZ", StringComparison.Ordinal)) return CellZ.Value;
        if (string.Equals(fieldId, "amount", StringComparison.Ordinal)) return Amount.Value;
        if (string.Equals(fieldId, "staminaCost", StringComparison.Ordinal)) return StaminaCost.Value;
        if (string.Equals(fieldId, "serial", StringComparison.Ordinal)) return Serial.Value;
        return null;
    }

    void IGeneratedComponent.WriteField(string fieldId, object? value, bool silent)
    {
        if (string.Equals(fieldId, "active", StringComparison.Ordinal))
        {
            if (silent) Active.SetSilent((bool)value!);
            else Active.Value = (bool)value!;
            return;
        }
        if (string.Equals(fieldId, "transaction", StringComparison.Ordinal))
        {
            if (silent) Transaction.SetSilent((string)value!);
            else Transaction.Value = (string)value!;
            return;
        }
        if (string.Equals(fieldId, "veinHex", StringComparison.Ordinal))
        {
            if (silent) VeinHex.SetSilent((string)value!);
            else VeinHex.Value = (string)value!;
            return;
        }
        if (string.Equals(fieldId, "sectionKey", StringComparison.Ordinal))
        {
            if (silent) SectionKey.SetSilent((ulong)value!);
            else SectionKey.Value = (ulong)value!;
            return;
        }
        if (string.Equals(fieldId, "cellOffset", StringComparison.Ordinal))
        {
            if (silent) CellOffset.SetSilent((int)value!);
            else CellOffset.Value = (int)value!;
            return;
        }
        if (string.Equals(fieldId, "cellX", StringComparison.Ordinal))
        {
            if (silent) CellX.SetSilent((int)value!);
            else CellX.Value = (int)value!;
            return;
        }
        if (string.Equals(fieldId, "cellY", StringComparison.Ordinal))
        {
            if (silent) CellY.SetSilent((int)value!);
            else CellY.Value = (int)value!;
            return;
        }
        if (string.Equals(fieldId, "cellZ", StringComparison.Ordinal))
        {
            if (silent) CellZ.SetSilent((int)value!);
            else CellZ.Value = (int)value!;
            return;
        }
        if (string.Equals(fieldId, "amount", StringComparison.Ordinal))
        {
            if (silent) Amount.SetSilent((int)value!);
            else Amount.Value = (int)value!;
            return;
        }
        if (string.Equals(fieldId, "staminaCost", StringComparison.Ordinal))
        {
            if (silent) StaminaCost.SetSilent((ulong)value!);
            else StaminaCost.Value = (ulong)value!;
            return;
        }
        if (string.Equals(fieldId, "serial", StringComparison.Ordinal))
        {
            if (silent) Serial.SetSilent((ulong)value!);
            else Serial.Value = (ulong)value!;
            return;
        }
    }

    bool IGeneratedSyncMetadata.TryGetSyncField(string fieldId, out ISyncField field)
    {
        if (string.Equals(fieldId, "active", StringComparison.Ordinal)) { field = Active; return true; }
        if (string.Equals(fieldId, "transaction", StringComparison.Ordinal)) { field = Transaction; return true; }
        if (string.Equals(fieldId, "veinHex", StringComparison.Ordinal)) { field = VeinHex; return true; }
        if (string.Equals(fieldId, "sectionKey", StringComparison.Ordinal)) { field = SectionKey; return true; }
        if (string.Equals(fieldId, "cellOffset", StringComparison.Ordinal)) { field = CellOffset; return true; }
        if (string.Equals(fieldId, "cellX", StringComparison.Ordinal)) { field = CellX; return true; }
        if (string.Equals(fieldId, "cellY", StringComparison.Ordinal)) { field = CellY; return true; }
        if (string.Equals(fieldId, "cellZ", StringComparison.Ordinal)) { field = CellZ; return true; }
        if (string.Equals(fieldId, "amount", StringComparison.Ordinal)) { field = Amount; return true; }
        if (string.Equals(fieldId, "staminaCost", StringComparison.Ordinal)) { field = StaminaCost; return true; }
        if (string.Equals(fieldId, "serial", StringComparison.Ordinal)) { field = Serial; return true; }
        field = null!;
        return false;
    }

    void IGeneratedComponent.ResetToDefault()
    {
        Active.SetSilent(false);
        Transaction.SetSilent(string.Empty);
        VeinHex.SetSilent(string.Empty);
        SectionKey.SetSilent(0UL);
        CellOffset.SetSilent(0);
        CellX.SetSilent(0);
        CellY.SetSilent(0);
        CellZ.SetSilent(0);
        Amount.SetSilent(0);
        StaminaCost.SetSilent(0UL);
        Serial.SetSilent(0UL);
    }
}
