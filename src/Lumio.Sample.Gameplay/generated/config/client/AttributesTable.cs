// <auto-generated>
// 生成物不得手改。由 LumioConfig `export --csharp-out` 重建。
// table: attributes
// target: C
// schemaFingerprint: fbdae4c62f0bc1f7c5d6aa584bb872a77553a88116881493c67415a4d17ade9f
// </auto-generated>

#nullable enable

using System;
using System.Collections.Generic;

namespace Lumio.Config.Generated.Client;

public readonly struct AttributesRow
{
    public AttributesRow(
        uint id,
        string name,
        long initial)
    {
        Id = id;
        Name = name;
        Initial = initial;
    }

    public uint Id { get; }
    public string Name { get; }
    public long Initial { get; }
}

public readonly struct AttributesTable
{
    private readonly AttributesRow[] _rows;
    private readonly Dictionary<uint, int> _index;

    public AttributesTable(IReadOnlyList<AttributesRow> rows)
    {
        if (rows is null)
        {
            throw new ArgumentNullException(nameof(rows));
        }

        var copy = new AttributesRow[rows.Count];
        for (var i = 0; i < rows.Count; i++)
        {
            copy[i] = rows[i];
        }

        Array.Sort(copy, static (left, right) => left.Id.CompareTo(right.Id));
        var index = new Dictionary<uint, int>(copy.Length);
        for (var i = 0; i < copy.Length; i++)
        {
            var id = copy[i].Id;
            if (!index.TryAdd(id, i))
            {
                throw new ArgumentException($"duplicate id {id}", nameof(rows));
            }
        }

        _rows = copy;
        _index = index;
    }

    public int Count => _rows.Length;

    public IReadOnlyList<AttributesRow> Rows => _rows;

    public bool TryGet(uint id, out AttributesRow row)
    {
        if (_index.TryGetValue(id, out var offset))
        {
            row = _rows[offset];
            return true;
        }

        row = default;
        return false;
    }
}
