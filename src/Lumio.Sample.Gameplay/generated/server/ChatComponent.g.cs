// <auto-generated/>
#nullable enable
using System;
using Lumio.GameRuntime.Ecs;

namespace Lumio.Sample.Gameplay.Components.Chat;

public sealed partial class ChatComponent : IGeneratedComponent, IGeneratedSyncMetadata
{
    partial void OnLastMessageTextChanging(string old, string @new, ChangeReason reason);
    partial void OnLastMessageTextChanged(string old, string @new, ChangeReason reason);
    partial void OnLastMessageTickChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnLastMessageTickChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnClientWrite(in SyncWrite w, ref bool accept);

    public partial void OnChatMessage(string line) => EmitClientRpc("OnChatMessage", Scope.Room, line);

    void IGeneratedComponent.BindFields(ISyncHost host)
    {
        LastMessageText = LastMessageText.Bound(host, this, 0, "ChatComponent.lastMessageText");
        LastMessageTick = LastMessageTick.Bound(host, this, 1, "ChatComponent.lastMessageTick");
    }

    void IGeneratedComponent.InvokePostAttribute() => PostAttribute();
    partial void PostAttribute();

    void IGeneratedComponent.InvokeFieldChanging(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnLastMessageTextChanging((string)oldValue!, (string)newValue!, reason);
        if (ordinal == 1) OnLastMessageTickChanging((ulong)oldValue!, (ulong)newValue!, reason);
    }

    void IGeneratedComponent.InvokeFieldChanged(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnLastMessageTextChanged((string)oldValue!, (string)newValue!, reason);
        if (ordinal == 1) OnLastMessageTickChanged((ulong)oldValue!, (ulong)newValue!, reason);
    }

    bool IGeneratedComponent.DispatchClientWrite(in SyncWrite write)
    {
        bool accept = true;
        OnClientWrite(in write, ref accept);
        return accept;
    }

    void IGeneratedComponent.DispatchServerRpc(string method, object?[] args)
    {
        if (method == "SendMessage")
        {
            SendMessage((string)args[0]!);
            return;
        }
    }

    void IGeneratedComponent.DispatchClientRpc(string method, object?[] args)
    {
    }

    void IGeneratedComponent.CapturePersist(IPersistWriter writer)
    {
        writer.WriteString("ChatComponent.lastMessageText", LastMessageText.Value);
        writer.WriteUInt64("ChatComponent.lastMessageTick", LastMessageTick.Value);
    }

    void IGeneratedComponent.CaptureSync(IPersistWriter writer)
    {
    }

    void IGeneratedComponent.RestorePersist(IPersistReader reader)
    {
        if (reader.TryReadString("ChatComponent.lastMessageText", out string lastMessageTextRestore))
            LastMessageText.SetSilent(lastMessageTextRestore);
        if (reader.TryReadUInt64("ChatComponent.lastMessageTick", out ulong lastMessageTickRestore))
            LastMessageTick.SetSilent(lastMessageTickRestore);
    }

    object? IGeneratedComponent.ReadField(string fieldId)
    {
        if (string.Equals(fieldId, "lastMessageText", StringComparison.Ordinal)) return LastMessageText.Value;
        if (string.Equals(fieldId, "lastMessageTick", StringComparison.Ordinal)) return LastMessageTick.Value;
        return null;
    }

    void IGeneratedComponent.WriteField(string fieldId, object? value, bool silent)
    {
        if (string.Equals(fieldId, "lastMessageText", StringComparison.Ordinal))
        {
            if (silent) LastMessageText.SetSilent((string)value!);
            else LastMessageText.Value = (string)value!;
            return;
        }
        if (string.Equals(fieldId, "lastMessageTick", StringComparison.Ordinal))
        {
            if (silent) LastMessageTick.SetSilent((ulong)value!);
            else LastMessageTick.Value = (ulong)value!;
            return;
        }
    }

    bool IGeneratedSyncMetadata.TryGetSyncField(string fieldId, out ISyncField field)
    {
        if (string.Equals(fieldId, "lastMessageText", StringComparison.Ordinal)) { field = LastMessageText; return true; }
        if (string.Equals(fieldId, "lastMessageTick", StringComparison.Ordinal)) { field = LastMessageTick; return true; }
        field = null!;
        return false;
    }

    void IGeneratedComponent.ResetToDefault()
    {
        LastMessageText.SetSilent(string.Empty);
        LastMessageTick.SetSilent(0UL);
    }
}
